//TODO Задача: Цифры из строки конвертировать в цифровой элемент
//? Теория: оператор + позволит конвертировать это.

/**
 *
 * @param {string} str строка с числом(и)
 * @returns {number} числовой элемент
 */
const stringToNumber = str => +str;