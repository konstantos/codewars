//TODO Задача: клонировать значение строки на определенное количество раз и вернуть как одну строку.
//? Теория: используем метод repeat(), который возвращает новую строку с содержанием стольких её копий, сколько было указано.


//* Практика:
const repeatStr = (n, str) => str.repeat(n); //Соединять ничего не надо, ибо возвращает как одну строку.